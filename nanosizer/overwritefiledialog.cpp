#include "overwritefiledialog.h"

OverwriteFileDialog::OverwriteFileDialog(QWidget* parent, QString filename) :
    QDialog(parent),
    ui(new Ui::OverwriteFileDialog)
{

    //ui->setupUi(this);

    //ui->SaveLabel->setPlainText("Overwrite " + filename "?");




}

